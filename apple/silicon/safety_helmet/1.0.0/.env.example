# Safety Helmet Detection Algorithm — Default Parameters
# Copy this file to .env to override defaults without changing code.
# Values here serve as baseline; runtime config_json can still override them.

conf_threshold=0.5
iou_threshold=0.45
model_path=weights/damoyolo_safety_helmet.onnx
